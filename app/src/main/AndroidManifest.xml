<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jetpackpractice">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackPractice">

        <activity android:name=".databinding.DataBindingActivity"
            android:exported="false"/>

        <activity android:name=".livedata.LiveDataActivity"
            android:exported="false"/>

        <activity android:name=".navigation.NaviActivity"
            android:exported="false"/>

        <activity android:name=".paging.PagingActivity"
            android:exported="false"/>

        <activity android:name=".paging.pagingex.PagingExActivity"
            android:exported="false"/>

        <activity android:name=".mvvm.ContactActivity"
            android:exported="false"/>

        <activity android:name=".mvvm.AddActivity"
            android:exported="false"/>

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>